// URL dictionary to map the url not present in megamenu data.

const URL_DICTIONARY = {
  pawri: [window.REACT_APP_SIMSIM_SERVICE, window.REACT_APP_PARTY_SERVICE],
};

export default URL_DICTIONARY;
